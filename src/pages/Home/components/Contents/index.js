import React from 'react';
import {
    Container,
    AddContentContainer,
    ContainerHeader,
    ContainerTitle,
    ContainerForm,
    InputBlock,
    Input,
    SubmitButton
  } from './styles';
import { useForm } from "react-hook-form";
import ContentBox from '../ContentBox';
const Contents = () => {
  const fields = ["ID", "Title", "Body", "Content Type", "Source Link"];
  return (
    <Container>

    </Container>
  );
}

export default Contents;